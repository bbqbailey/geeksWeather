var rootPath = require("geeksweatherconfig").rootPath;
var Myapp = require(rootPath + "/public/tools/myapp");

var expect = require('chai').expect;
var sinon = require('mocha-sinon');
var assert = expect.assert;
/*describe("Testing nedb", function() {
  it("here we go with the call", function() {
    var myapp = new Myapp(2016, "Mar");
    myapp.getAllData(function(err, docs) {
      if(err) {
        console.log('testUseMyapp.js getAllData error: ', err);
      } else {
        console.log('testUseMyapp.js getAllData docs: ', docs);
      }
    });
  });
});
*/
console.log('=====================================================\r\n' +
            '==          This doesn\'t work for some reason     ==\r\n' +
            '==          with mocha.  Test with useMyapp.js     ==\r\n' +
            '=====================================================\r\n');
